version: '3'
services:
  postgres:
    container_name: ontology-pg
    restart: always
    image: postgres:14.4
    ports:
      - "35432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=iecas
      - POSTGRES_PASSWORD=iecas123


