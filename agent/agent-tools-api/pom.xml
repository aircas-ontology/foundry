<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>agent-tools-api</artifactId>
    <packaging>jar</packaging>

    <parent>
        <groupId>com.aircas.ptr</groupId>
        <artifactId>agent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <description>
        Agent 工具契约模块：定义 ontology-server 原子操作对外暴露的工具接口与传输模型（ToolVO）。
        ontology-server 依赖本模块并实现接口（以 REST 暴露）；agent-server 依赖本模块并消费接口（包装为 Spring AI @Tool）。
        本模块保持零业务依赖，仅含纯 Java 接口与 record，避免循环依赖。
    </description>

    <!-- 契约模块不引入任何业务/框架依赖，保证被 ontology-server 与 agent-server 双向安全依赖 -->

</project>
