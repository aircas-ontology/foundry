server:
  port: 36002
  servlet:
    context-path: /ontology

spring:
  application:
    name: ontology
  datasource:
    main:
      driver-class-name: org.postgresql.Driver
      #url: jdbc:postgresql://${PG_DB_HOST}:${PG_DB_PORT}/${PG_DB_NAME}?currentSchema=ontology
      url: jdbc:postgresql://192.168.9.17:35432/postgres?currentSchema=ontology
      username: iecas
      password: iecas123
      validation-query: SELECT 1
      test-on-borrow: true
    datalake:
      driver-class-name: org.postgresql.Driver
      url: jdbc:postgresql://192.168.2.173:5432/foudry_datasource
      username: postgres
      password: postgres

mybatis:
  configuration:
    lazy-loading-enabled: true
    aggressive-lazy-loading: false
    map-underscore-to-camel-case: true
  mapper-locations: classpath*:/mybatis-mapper/*Mapper.xml

#eureka:
#  instance:
#    prefer-ip-address: true
#    lease-renewal-interval-in-seconds: 5
#    lease-expiration-duration-in-seconds: 30
#    secure-port-enabled: false
#    non-secure-port-enabled: true
#  client:
#    register-with-eureka: true
#    registry-fetch-interval-seconds: 5
#    fetch-registry: true
#    service-url:
#      defaultZone: http://localhost:36000/eureka/

ontology:
  swagger:
    enable: true

elasticsearch:
  addr: 192.168.9.101:9200