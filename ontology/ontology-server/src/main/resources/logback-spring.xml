<?xml version="1.0" encoding="UTF-8" ?>

<configuration scan="false">

    <!--定义项目名称-->
    <springProperty scope="context" name="PROJECT_NAME" source="spring.application.name"/>
    <!-- 定义日志文件存储位置 -->
    <property name="LOG_HOME" value="./logs/${PROJECT_NAME}"/>

    <!-- 设置控制台日志输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>[%d{yyyy-MM-dd HH:mm:ss.SSS}] [%thread] [%X{logId}] [%-5level] %logger{36} - [%file:%line] %msg%n </pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 设置所有级别的日志输出到一个文件 -->
    <appender name="ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <Prudent>true</Prudent>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 日志保留天数 -->
            <MaxHistory>30</MaxHistory>
            <fileNamePattern>${LOG_HOME}/%d{yyyy-MM-dd}/all.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <!-- 日志文件的最大大小 -->
                <MaxFileSize>10MB</MaxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>[%d{yyyy-MM-dd HH:mm:ss.SSS}] [%thread] [%X{logId}] [%-5level] %logger{36} - [%file:%line] %msg%n </pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 设置根日志级别 -->
    <root level="INFO">
        <appender-ref ref="ALL"/>
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
